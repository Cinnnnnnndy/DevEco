# 15 跨端接续 · 消融记录（终轮）

- **没有 Agent**：离开前自己记下断点、HiLog 看到哪行、改到哪；回来重连真机、重找断点、翻日志、翻聊天。
- **有 Agent**：Agent·调试 改完 loadCover、跑到断点，停在第 4 步等你批（琥珀）；你锁屏离开，任务变成手机实况窗，在手机上就能批；回来碰一下电脑，真机、断点 28、HiLog 行、它的步骤原位接上。
- **多 Agent**：同时 Agent·回归 要借 Mate X6 跑回归，和第 5 步撞设备。两件放进一个「在等你」队列，离开时整队带走（实况窗只推最挡路的一件，写「还有 1 件」；接续卡写「在等你 2 件」），回来队列原样还在。

| 元素 | 结果 | 依据 |
|---|---|---|
| 左栏「在等你」队列（Agent·调试 批第 4 步 / Agent·回归 借 X6） | 新增 | 多 Agent 时要带走的是整个队列，不止一个对话；借 X6 的决定直接影响第 5 步在几台上跑 |
| 顶栏 Agent 胶囊（DemoFrame.agent） | 新增，替代状态栏胶囊 | 只推最挡路的一件 +「还有 N 件」，点它跳到在等你那处 |
| 状态栏自造胶囊「任务在 MateBook · 另外 3 端已同步」 | 删除 | 和「这个任务也在另外 3 端」卡片说的是同一件事，顶栏胶囊已承担状态 |
| 「各端最近的动作」卡片（4 条动作流水） | 删除 | 挂不到任务：离开 / 接续时你要的是「现在到哪了」，不是流水；到哪了已写在 3 端卡片页脚和轨迹里 |
| 接续后对话里的回执卡（4 行 + 跳到 / 打开按钮） | 合并成一行 `.ai-toolrow` | 接续时编辑器已自动定位到 28 行、HiLog 已打开到原行，回执卡上的按钮重复这两个动作 |
| 5 步步骤表 | 合并：做完的 3 步收成一行 `.ai-toolrow`（可展开），第 4 步改成琥珀审批卡 `.ai-approve`（带 diff、批准 / 打回），第 5 步一行 | 工具调用收成一行；「在等你」要单独一张，而且在 IDE 里真能批（原来只写「或在这里点」却没有按钮） |
| 编辑器 27–28 行 | 改为 Agent 写的异步版本 + `.ai-block/.ai-gut`（来源「Agent·调试 · 第 2 步 · 待你批」） | 原来编辑器里还是同步旧代码，和「第 2 步已改」对不上；AI 写的行要带来源 |
| 任务飞入动画（`.fly` 图标从电脑飞到手机再飞回） | 删除 | 纯装饰动效；锁屏 → 实况窗出现、碰一碰波纹已表达「过去了 / 回来了」 |
| 锁屏壁纸的紫蓝渐变（电脑、手机） | 删除，改纯色 | 装饰渐变 |
| 接续卡双层阴影 + 描边、信息块描边 | 改为单层阴影 + 填充 | 描边改填充、不叠阴影 |
| 手机实况窗 / 状态栏胶囊 / 碰一碰提示的品牌蓝 | 改中性；胶囊点改琥珀 | 蓝只留主按钮；胶囊表达的是「在等你」 |
| 平板审批按钮的蓝描边与蓝字 | 改中性填充，亮起时才用主色 | 同上 |
| 「已同步」蓝色胶囊、选中行蓝底、HiLog 当前行蓝字 | 改中性 | 蓝色只留选中标记与主按钮 |
| Google Fonts 链接、700 字重 | 删除 / 改 600 | 字体走 tokens |
| 引导标注 5 条（含手表两条） | 减到 4 条 | 手表是次路径；补一条多 Agent 队列 |
| 碰一碰手机 / 平板 · 手表两个场景、锁屏离开主按钮、接续卡、HiLog 原行 | 保留 | 主流程与次路径本身 |
