# 30 折叠屏开发 · 消融记录

- 没有 Agent：在 MateBook Fold 上自己排 IDE、模拟器和调试窗；看到价格被裁，截图发给 AI 还得描述是哪张卡、哪个组件，再手抄改法、重跑两种屏确认。
- 有 Agent：折到悬停，下屏自动摆好 sm / lg 两个本机窗口和调试台；用 M-Pen 圈一处，Agent·布局 读圈选 + 组件树 + 截图，定位 ProductCard.ets:17 给出改法并停下等你（琥珀）→ 你接受后它改两行、热重载两个窗口、比对 sm / lg 截图；它干活的进度同时显示在下屏（你眼睛正看着的那块屏）。
- 多 Agent：下屏只放和上屏当前文件相关的 Agent——切到 ProductCard.ets 看到 Agent·布局，单步进入 CartStore.ets 看到 Agent·回归（你在调试本机窗口，它的单测让路、继续后接着跑）；不相关的 Agent·多语言收成一行「其余 N 个在跑，不碰当前文件」。

| 元素 | 结果 | 依据 |
|---|---|---|
| 状态栏「本机运行 · 2 个窗口 / 已暂停 …」胶囊 | 删除 | 运行 / 暂停态已在窗口暂停层、调试台状态条上；状态栏中间让给顶栏 Agent 胶囊 |
| 顶栏「展开 180° / 悬停 110° · 下屏运行」姿态胶囊 | 删除 | 左侧姿态单选与折叠屏本身已表达；悬停时上屏只有 780px，腾给 Agent 胶囊 |
| 对话开头「本机：MateBook Fold · HarmonyOS 6.1 · 展开 180°」 | 删除 | 与左侧设备卡重复 |
| 桌面与折叠屏壁纸的蓝 / 绿径向光 | 删除 | 纯装饰，且大面积用了蓝 |
| 姿态按钮、调试台按键、虚拟键盘、调试台的 1px 描边与蓝色选中框 | 删除 | 改填充；选中用中性选中底 |
| 热重载的蓝色描边闪烁与蓝底提示 | 合并 | 改成功绿描边一次 + 中性深色提示 |
| 圈选回答「读了圈选…」+ 大段说明 + 两个按钮 | 合并 | 工具行 + 结论先行的回答卡 + 琥珀「在等你 · 接受后两个窗口热重载并比对」 |
| 修复步骤卡（三步 steps 表） | 合并 | 收成三行工具行，一行一步 |
| 改过的第 17、20 行绿色高亮 | 合并 | 改为 ai-block 竖条 + 悬停看来源（Agent·布局 · M-Pen 圈选后改） |
| 引导标注 8 条 | 合并 | 留 5 条：折到悬停、改 sm 列宽、点加购命中断点、笔圈给 Agent、它停在这等你 |
| 下屏 Agent 进度卡 + 其余一行 | 新增 | 回答「它在干什么」而且放在你眼睛所在的下屏，按当前文件过滤 |
| 上屏写码 / 下屏运行、断点时下屏变调试台、M-Pen 圈选 | 保留 | 本页主张 |
