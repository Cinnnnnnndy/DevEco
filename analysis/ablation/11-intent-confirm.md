# 11 意图确认 · 消融记录（终轮）

- **没有 Agent**：你自己读 LoginPage、ErrorMap、string.json，想清改几处、会不会碰公共模块，再动手；大需求写在文档里自己拆步骤。
- **有 Agent**（本页本来就是 Agent 形式，这轮只做统一）：它先把理解和改法摆出来等你点（琥珀「在等你」），等你的时候照读文件；大需求先出可改的计划（带模块拓扑、关键流程、验证方式），你改完放行，它照做、偏离就地说明；写入的行带来源。
- **多 Agent**：计划的第 2 步要改 `string.json`，而 Agent·多语言 正在改它 → 计划卡「别的 Agent」一行、步骤里文件旁的来源标记、风险里写明「执行到这一步先等它提交，再在它的版本上加两条」；执行时胶囊变「等 Agent·多语言 提交」，完成后对话里记一行。两件都在等你时，胶囊只推计划那件并写「还有 1 件」。

| 元素 | 结果 | 依据 |
|---|---|---|
| 状态栏两个自造胶囊（等你确认 · 改错误提示 / 写计划 · 按计划执行，带进度条） | 合并 | 统一到顶栏 Agent 胶囊（ai.css 状态机）：两件在等你时只推一件 + 「还有 N 件」；执行 / 等别的 Agent / 完成各有状态 |
| 确认卡的蓝色描边外发光（`.dm-card.accent`）与问号图标 | 合并 | 这是「在等你」：改成琥珀细描边 + 琥珀点，全页只有它抢注意力 |
| 计划卡的蓝色描边外发光 | 删除 | 同上；只在待你确认时加琥珀描边 |
| 对话计划卡里的蓝色「按计划执行」主按钮 | 合并 | 与编辑器计划栏的主按钮重复，一屏只留一个主按钮；对话里的降为次按钮 |
| 「等你的时候没停」前的蓝色闪电 | 合并 | 改成智能色小环（进行中），读完变勾 |
| 编辑器第 28 行改动的绿色 `hl-ok` 底 | 合并 | 改成 `.ai-block` 来源竖条，悬停看是谁写的 |
| 右侧 Project 文件树（默认展开，宽 440–580 px） | 删除（默认收起，侧栏仍可开） | 会动哪些文件已在计划的改动地图与步骤里；手机接续时也不再压住它（遗留问题「phone covers project tree」一并解决），手机出现时计划文档右侧留出位置 |
| 地图提示里的「已在项目树选中」 | 删除 | 文件树默认收起，这句不再成立 |
| DevEco Code 面板底部免责声明 | 删除 | 挂不到任务 |
| 计划文档随编辑器变宽把模块图放大到 1 000 px | 修复 | 文档限宽 800 px，图保持可读尺寸 |
| 改动地图里「改动模块」蓝框蓝底、分隔线、悬停 / 连线高亮的蓝；执行中步骤的蓝色转圈与蓝底；闪烁的蓝底；「读到后补的」蓝色标签；手机推送光束的蓝底 | 合并到中性 / 智能色 | polish 3：蓝只留主按钮与选中；进行中用 ai.css 智能色 |
| 选项卡片、步骤列表、地图外框的 1px 描边；手机锁屏渐变壁纸；手机说明条描边 | 删除 | polish 2 / 5 |
| Google Fonts（Inter / JetBrains Mono）；手机实况窗 700 字重 | 删除 / 降到 600 | polish 1 |
| 引导 8 条 | 合并为 5 条 | 确认改法 → 切 Plan → 计划写明会碰谁的文件 → 执行 → 推到手机 |
| Build / Goal / Plan 模式、可拖拽可改的计划、要你决定的两问、验证勾选、偏离说明、模块拓扑 / 关键流程点击跳步骤、推到 Mate 70 Pro 确认与实况窗 | 保留 | 本页主张，全部可点 |
