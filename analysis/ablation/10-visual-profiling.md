# 10 可视化调优 · 消融记录（终轮）

- **没有 Agent**：你录 trace，自己在 CPU / 内存 / 并发 / 多设备之间切，点火焰图找到 `ListPage.ets:28`，手改，再录一次对比。
- **有 Agent**：你把记录 #12 交给 Agent·调优，它读完火焰图、内存、并发轨迹，把结论先行写在面板里、在图上标「Agent·调优 圈出」；停在「改这一行并复测」等你（琥珀）；你点一下，它写入带来源的 6 行，在 Mate 70 Pro 上重放同一手势，前后对比落在 Profiler 里。点火焰图、切视图、点泳道都退为「接手」。
- **多 Agent**：复测要独占真机，而 Agent·回归 正在 Mate 70 Pro 上跑 UI 用例（负载会污染帧率）→ 在等你里给「改并复测（回归先暂停）/ 等回归跑完」，Profiler 顶栏常驻真机租约；并发泳道里标出 `prefetchNextPage` 是 Agent·预取 昨天加的任务，代码行带它的来源。

| 元素 | 结果 | 依据 |
|---|---|---|
| Agent 面板里「已看过记录 #12」+ 长段落 + 两个 chip（出补丁并自动复测 / 先看调用栈） | 合并 | 换成一行工具调用 + 结论先行回答卡 + 琥珀审批卡（改并复测 / 先看调用栈 / 等回归跑完） |
| 复测步骤列表（出补丁 / 已应用 / 重跑，三行 steps） | 合并 | 过程收成一行 `.ai-toolrow`，完成后给结论卡 |
| 状态栏「记录 #12 · 掉帧 9 · Mate 70 Pro」胶囊 | 删除 | 与 Profiler 顶栏记录 / 设备、顶栏 Agent 胶囊重复；状态只留顶栏胶囊一处 |
| 「复测就绪 · 会用同一条记录」胶囊 | 合并 | 改成真机租约「真机被 Agent·回归 占用 · 12 / 38」，随复测变「回归已暂停 / 已恢复」 |
| 编辑器标签栏「图 ↔ 代码联动」蓝色开关 | 删除 | 不可操作的常开开关，联动本身在点图时已看得见；也是一处多余的蓝 |
| DevEco Code 面板底部免责声明 | 删除 | 挂不到这一页任务 |
| 右侧 Project 文件树（默认展开，宽 360–440 px） | 删除（默认收起，侧栏仍可开） | 这一页从图到行、从 Agent 到行，都不经过文件树；让出宽度给代码与 Profiler |
| 补丁行的绿色 `hl-ok` 底 | 合并 | 改成 `.ai-block` 来源竖条，悬停看是谁写的；人改的不打标 |
| 火焰图、内存曲线、TaskPool 执行 / 排队色块、多设备帧柱、断点标签的蓝色系列 | 合并到中性灰 | polish 3：图表系列不用蓝；红 / 黄 / 绿只表达状态 |
| 图表卡、泳道、结论条、问题胶囊的 1px 描边；迷你设备阴影；本机窗口双层阴影；碰一碰卡描边与蓝色波纹 | 删除 | polish 2 / 5：填充分层，窗口只留一层阴影 |
| Google Fonts（Inter / JetBrains Mono） | 删除 | 字体走 tokens |
| 引导 8 条 | 合并为 5 条 | 从任务出发：改并复测 → 真机租约 → 接手点火焰图 → 三台一起比 → 同采 |
| CPU / 内存 / 电量 / 并发 / 多设备视图、火焰图点选落行、并发泳道与「交给 Agent 出补丁」、碰一碰连 MatePad、本机自由窗口、三台同采对齐 | 保留 | 手动次路径与鸿蒙电脑能力，全部可点 |
