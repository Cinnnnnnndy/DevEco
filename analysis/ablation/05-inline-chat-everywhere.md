# 05 就地对话 · 消融记录（终轮）

- 没有 Agent：构建失败、崩溃栈、测试机截图都要复制进聊天窗，描述「是哪一行」，拿到答案再跳回原处手改、重建、自己装机复测。
- 有 Agent：在出问题的那处点一下（Build / Debug / HiLog / 编辑器 / 预览），它读现场、结论先行给补丁；改到别人的地盘就停下问你（琥珀）；你接受后它重建、装机复测，写进文件的行带来源、可退回。
- 多 Agent：构建报错的 `ProductApi.ets:12` 和要改的 `module.json5` 是后台 **Agent·接口** 正在跑的任务写的——回答里标出来源，补丁不偷偷改它的文件，先问你「并进它的任务 / 只在这里改」。

| 元素 | 结果 | 依据 |
|---|---|---|
| README.md 就地对话（文档改口吻） | 删除 | 和 LoginPage 就地对话是同一个动作（选中 → 问 → diff → 接受），挂不到「修崩溃」这件事；重复只留代码里那一处 |
| Profiler 面板与「打开 Profiler」按钮 | 删除 | 帧耗时和本页任务（构建 → 断点 → 崩溃 → 复测）无关，只是一张静态卡 |
| 鸿蒙电脑桌面 + 小艺唤起（渐变桌面、玻璃 Dock、彩虹光球） | 删除 | 结果同样是「打开 DetailPage.ets:24」，HiLog 与手机现场已覆盖；属纯展示性的系统能力演示，且是自造 AI 渐变 / 光效 |
| DevEco Code 欢迎区里的「叫小艺」提示卡 | 删除 | 随上一条一起删 |
| 截图、日志「飞进输入框」的动画 | 删除 | 附件直接落进输入框已说明结果；能不动的不动 |
| 手机碰一碰的扩散光环 | 删除 | 装饰动效；手机贴近的位移已表达碰一碰 |
| 预览里按钮与对话框之间的绿色虚线 | 删除 | 对话框标题已写「Button（LoginPage.ets:16）」，改为 ai.css 的 AI 选中虚线框 + 角标 |
| 状态栏里的 dm-capsule 进度 | 合并 | 与顶栏 Agent 胶囊重复，只留顶栏一处（ai.css 状态机） |
| 工具窗 AI 卡片头部的红 / 绿 pill（「缺少权限声明」「BUILD SUCCESSFUL」「JS_ERROR」） | 删除 | 回答首句已说结论 |
| 工具窗 AI 卡片左侧蓝紫渐变竖条、描边、阴影 | 删除 | 自造 AI 色；改成 ai.css 回答卡 / 审批卡（填充分层） |
| 「读了 Build 输出 / module.json5 …」一排小标签 | 合并 | 收成一行 `.ai-toolrow`（动词 + 目标 + 耗时） |
| 就地对话的 diff 上下文行（`} catch (e) {` / `}`） | 删除 | 只留一删一增两行，弹层不再压到编辑器外 |
| 就地对话「重新生成」 | 删除 | 只有 README 用，随 README 删 |
| 下一处编辑：蓝色幽灵行底、蓝标记、蓝色 tag、虚线 peek、描边挂件 | 保留 · 改色 | 这是 AI 建议，改用 `--ai-tint` / `--ai-ink`；tag、peek、挂件改中性填充 |
| 下一处编辑：接受后的绿色闪烁 | 合并 | 改成 `.ai-block` 接受后 3 s 淡出，悬停竖条看来源 |
| 「已同步 CartItem.ets:5」绿色 inlay、蓝色跨文件签 | 合并 | 统一成 `.ai-src` 来源标记 |
| 就地对话的 Accept 主按钮（蓝） | 保留 · 降级 | 每屏只留一个主按钮给 Agent 在等你的那一步；接受改普通按钮 |
| Google Fonts `<link>`、700 字重 | 删除 | 字体走 tokens，字重 ≤ 600 |
| 预览设备「Phone · 1080×2340」 | 保留 · 改 | 换成 Mate 70 Pro 1260×2844（sm），外框按比例 |
| Build / Debug / HiLog 就地回答、下一处编辑、手机现场 → 补丁 → 复测 | 保留 | 本页主张本身 |
| 标注 10 条 | 合并为 5 条 | 按任务：Agent 在等你 → 上真机就地问 → 编辑器就地改 → 下一处编辑 → 手机现场 |
