# 02 场景化体检 · 消融记录（终轮：Agent 化）

- 没有 Agent：你在 App Check 里逐条点开问题、读原因、自己改（或一条条点「让 DevEco Code 改」），改完再起模拟器回归。
- 有 Agent：点「交给 Agent 修」，Agent·体检 按上架规则扫全工程，每条出补丁；机械改动默认勾上，产品取舍（隐私用途、错误提示文案）标「要你看」；你勾选后点「接受 N 条并回归」，它写入、在本机窗口回归、上架自检重跑，修好的从列表消失。逐条「只修这条 / 忽略这条」与其他场景页签、用户旅程保留为次路径。
- 多 Agent：无障碍补丁和 Agent·多语言 改到 LoginPage.ets 同一行——它先 rebase 到对方之后，把合并结果放进琥珀审批卡问你「用合并后的 / 这条等它做完再说」。

| 元素 | 结果 | 依据 |
|---|---|---|
| 状态栏「体检 · 14 条待处理」胶囊 | 删除 | 与页签计数重复；顶栏换成 Agent 胶囊（状态 + 点它回到在等你处） |
| 详情里蓝色主按钮「让 DevEco Code 改」 | 合并 | 改为次级「只修这条」；主按钮让给「交给 Agent 修 / 接受 N 条并回归」，每屏一个 |
| 列表 / 详情里蓝色「DevEco Code 在改」胶囊 | 合并 | 统一为 ai.css 来源标记（星芒 + Agent·体检 在改） |
| 旅程里的 i-genie 图标 | 合并 | 换成 ai-icons 的 ‹✦› 变体（拆步骤 / 修复） |
| dm-pill.info 蓝底（在跑、自动重跑） | 合并 | 改中性填充；蓝只留主按钮、选中、链接、焦点 |
| 旅程「在跑」行的蓝色左边 + 蓝底、步骤号蓝底、缩略图蓝框 | 合并 | 改中性灰；在跑不是选中 |
| 断点签 md / lg 蓝底、设备芯片选中蓝框 + 蓝底 | 合并 | 改中性；复选框保留蓝色勾 |
| 碰一碰卡：蓝色涟漪、蓝色虚线感应区、手机蓝色渐变屏、描边 + 重阴影 | 合并 | 改中性，只留一层阴影 |
| 本机窗口两层阴影、窗口下方尺寸条的描边与阴影 | 合并 | 一层阴影 / 纯填充 |
| 应用内横幅、品牌卡、商品大图的渐变 | 合并 | 改纯色（示例应用自身的装饰） |
| 真机镜像外框阴影 | 删除 | 装饰 |
| HiLog 框描边 | 删除 | 用填充区分 |
| Devices 面板 480 px | 合并 | 降到 420 px，给编辑器留宽（final-notes 遗留） |
| Google Fonts `<link>`、700 字重 | 删除 | 字体走 tokens，字重 ≤600 |
| 引导标注 8 条 | 合并 | 留 4 条：交给 Agent、接受并回归、撞同一行、写旅程 |
| ERR_AUTH_401 那条定位到第 27 行（其实是 `catch`） | 合并 | 改到真正的第 28 行 |
| 新增：Agent 条（扫 → 出补丁 → 写入 → 回归，工具调用各一行）、补丁勾选、「要你看」琥珀签、补丁 diff、冲突审批卡、`.ai-block` 行 | 保留 | Agent 主流程必需：它做了什么、哪些要你判断、在哪等你、改到哪 |
| 首次启动 / 低端机 / 无网弱网页签、用户旅程（本机窗口 + 碰一碰真机并行） | 保留 | 原有能力，作为次路径 |
